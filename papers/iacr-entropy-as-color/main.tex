% IACR ePrint Submission
% Entropy as Color: A GF(3) Algebraic Framework for Composable Cryptographic Entropy Sources
\documentclass[preprint]{iacrtrans}

\usepackage{amsmath,amssymb,amsfonts}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{hyperref}
\usepackage{cleveref}

% Color definitions for GF(3) visualization
\definecolor{tritplus}{RGB}{255,100,100}   % Warm - PLUS (+1)
\definecolor{tritergodic}{RGB}{100,200,100} % Neutral - ERGODIC (0)
\definecolor{tritminus}{RGB}{100,100,255}  % Cool - MINUS (-1)

% QCD Color definitions
\definecolor{qcdred}{RGB}{220,60,60}
\definecolor{qcdgreen}{RGB}{60,180,60}
\definecolor{qcdblue}{RGB}{60,60,220}
\definecolor{qcdantired}{RGB}{60,180,180}
\definecolor{qcdantigreen}{RGB}{180,60,180}
\definecolor{qcdantiblue}{RGB}{180,180,60}

\title{Entropy as Color: GF(3) Labels for Auditable Entropy-Source Composition}

\author{
  Anonymous\inst{1}
}
\institute{
  Anonymous Institution
}

\begin{document}

\maketitle

\begin{abstract}
We study a deterministic presentation and audit layer that assigns elements of the finite field $\mathbb{F}_3$ and corresponding color tiles to observations from heterogeneous entropy sources. The labels make policy balance and execution-order invariance inspectable, but they neither create entropy nor establish cryptographic security. We define the label algebra, prove its elementary conservation identities, and connect those identities to a Julia implementation and Lean statements. We also state the source-model, extractor, and reduction obligations that would be required to turn this audit layer into a cryptographic construction. Our principal contribution is therefore a falsifiable separation between measured source evidence, deterministic representations, and security claims.
\end{abstract}

\keywords{Entropy Sources \and GF(3) \and Audit Labels \and Reproducibility \and Randomness Extraction}

%------------------------------------------------------------------------------
\section{Introduction}
\label{sec:intro}
%------------------------------------------------------------------------------

Cryptographic systems depend fundamentally on high-quality entropy sources, yet the composition and verification of entropy from heterogeneous sources remains largely ad-hoc. Current approaches treat entropy as a scalar quantity (measured in bits), losing critical structural information about source independence, quality degradation, and compositional properties.

We propose a paradigm shift: treating entropy sources as \emph{colored} objects in an algebraic structure derived from the finite field $\text{GF}(3) = \{-1, 0, +1\}$. This ``entropy as color'' framework draws deep inspiration from quantum chromodynamics, where color charge conservation governs fundamental particle interactions.

\paragraph{Contributions.}
\begin{enumerate}
    \item \textbf{GF(3) Audit Algebra}: We define ternary labels with operation $\oplus_3$ and separate their algebraic invariants from entropy estimates (\Cref{sec:algebra}).
    
    \item \textbf{Representation Boundary}: We map labels to color tiles while making explicit that colors and hashes represent observations rather than identify sources or grant authority (\Cref{sec:color}).
    
    \item \textbf{Algebraic Scope}: We identify the additional typing and naturality obligations needed for a Yang--Baxter or braided-monoidal interpretation (\Cref{sec:yangbaxter}).
    
    \item \textbf{Security Non-implication}: We show why label conservation alone implies neither extraction nor composable security and give the missing proof obligations (\Cref{sec:security}).
    
    \item \textbf{Executable Reference}: We connect deterministic label composition to the Gay.jl implementation and its tests (\Cref{sec:implementation}).
\end{enumerate}

%------------------------------------------------------------------------------
\section{Preliminaries}
\label{sec:prelim}
%------------------------------------------------------------------------------

\subsection{Notation}

Let $\mathbb{F}_3 = \text{GF}(3) = \{-1, 0, +1\}$ denote the finite field of three elements with addition modulo 3. We use the term \emph{trit} for elements of $\mathbb{F}_3$, with semantic labels:
\begin{align}
    \text{MINUS} &\triangleq -1 & \text{(validator/constrainer)} \\
    \text{ERGODIC} &\triangleq 0 & \text{(coordinator/synthesizer)} \\
    \text{PLUS} &\triangleq +1 & \text{(generator/executor)}
\end{align}

\subsection{Entropy Sources}

An \emph{entropy source} $\mathcal{E}$ is characterized by:
\begin{itemize}
    \item Output distribution $D$ over bitstrings
    \item Min-entropy rate $H_\infty(\mathcal{E})$
    \item Sampling rate $r$ (samples per second)
    \item Independence structure $\mathcal{I}$ (correlation with other sources)
\end{itemize}

\subsection{Color Charge in QCD}

In quantum chromodynamics, quarks carry one of three color charges $\{\text{red}, \text{green}, \text{blue}\}$ or their anti-colors. The fundamental constraint is \emph{color confinement}: observable hadrons must be color-neutral (``white'').

%------------------------------------------------------------------------------
\section{GF(3) Entropy Algebra}
\label{sec:algebra}
%------------------------------------------------------------------------------

\begin{definition}[Trit]
A \emph{trit} is an element $t \in \mathbb{F}_3 = \{-1, 0, +1\}$ with addition defined as:
\[
a \oplus_3 b \triangleq ((a + b + 1) \mod 3) - 1
\]
\end{definition}

\begin{definition}[Trit Assignment]
Given an entropy source $\mathcal{E}$, its \emph{trit assignment} $\tau(\mathcal{E}) \in \mathbb{F}_3$ is determined by:
\[
\tau(\mathcal{E}) = \begin{cases}
    +1 & \text{if } H_\infty(\mathcal{E}) > \theta_{\text{high}} \\
    0 & \text{if } \theta_{\text{low}} \le H_\infty(\mathcal{E}) \le \theta_{\text{high}} \\
    -1 & \text{if } H_\infty(\mathcal{E}) < \theta_{\text{low}}
\end{cases}
\]
where $\theta_{\text{low}}, \theta_{\text{high}}$ are application-specific thresholds.
\end{definition}

\begin{proposition}[Conditional label balance]
\label{thm:conservation}
For labels $a,b,c\in\mathbb{F}_3$, if $c=-(a+b)$, then:
\[
a \oplus_3 b \oplus_3 c = 0.
\]
This algebraic statement does not imply that three measured sources are independent, unpredictable, or secure.
\end{proposition}

\begin{proof}
By substitution, $a+b+c=a+b-(a+b)=0$ in the additive group of $\mathbb{F}_3$. See \Cref{app:proofs} for the exact Lean cross-reference.
\end{proof}

%------------------------------------------------------------------------------
\section{Color Charge Formalism}
\label{sec:color}
%------------------------------------------------------------------------------

Building on the trit algebra, we introduce a richer color structure inspired by QCD.

\begin{definition}[Color Charge]
An entropy source $\mathcal{E}$ carries a \emph{color charge} $\chi(\mathcal{E}) \in \mathcal{C}$ where:
\[
\mathcal{C} = \{\text{RED}, \text{GREEN}, \text{BLUE}, \text{ANTI\_RED}, \text{ANTI\_GREEN}, \text{ANTI\_BLUE}, \text{COLORLESS}\}
\]
\end{definition}

The color-to-trit mapping $\phi: \mathcal{C} \to \mathbb{F}_3$ is:
\begin{align}
    \phi(\text{RED}) = \phi(\text{ANTI\_RED}) &= +1 \\
    \phi(\text{GREEN}) = \phi(\text{ANTI\_GREEN}) &= 0 \\
    \phi(\text{BLUE}) = \phi(\text{ANTI\_BLUE}) &= -1 \\
    \phi(\text{COLORLESS}) &= 0
\end{align}

\begin{definition}[Color Neutrality]
A set of entropy sources $\{\mathcal{E}_1, \ldots, \mathcal{E}_n\}$ is \emph{color-neutral} if:
\[
\bigoplus_{i=1}^n \phi(\chi(\mathcal{E}_i)) = 0 \pmod{3}
\]
\end{definition}

\paragraph{Visual Representation.}
Color charges map to the HSL color wheel:
\begin{itemize}
    \item \textbf{PLUS (+1)}: Warm hues (0°--60°, 300°--360°)
    \item \textbf{ERGODIC (0)}: Neutral hues (60°--180°)
    \item \textbf{MINUS (-1)}: Cool hues (180°--300°)
\end{itemize}

%------------------------------------------------------------------------------
\section{Yang--Baxter Scope}
\label{sec:yangbaxter}
%------------------------------------------------------------------------------

The label set suggests braid-like visualizations, but a visualization is not a
braiding on entropy sources. We record the candidate below to expose, rather
than conceal, the missing structure.

\begin{definition}[R-Matrix]
The candidate scalar kernel $r: \mathcal{C} \times \mathcal{C} \to \mathbb{C}$ is defined by:
\[
r(i,j) = q^{\phi(\chi_i) \cdot \phi(\chi_j)}
\]
where $q = e^{2\pi i/3}$ is a primitive third root of unity.
\end{definition}

This scalar kernel is not yet an operator $R:V\otimes V\to V\otimes V$ and
therefore does not, by itself, state the Yang--Baxter equation. Establishing a
braided monoidal category would require a category of source objects, a
monoidal product, a natural invertible braiding, and proofs of the hexagon
identities. We make no such theorem in the present work.

%------------------------------------------------------------------------------
\section{Security Analysis}
\label{sec:security}
%------------------------------------------------------------------------------

\begin{proposition}[Label conservation is not extraction]
\label{thm:security}
GF(3) label balance alone implies no lower bound on min-entropy and no
computational indistinguishability guarantee.
\end{proposition}

\begin{proof}
Take three deterministic constant sources and assign them labels $+1,0,-1$.
Their labels balance, while their joint output has min-entropy zero. Thus label
balance cannot imply entropy amplification, pseudorandomness, or universal
composability.
\end{proof}

A cryptographic construction would additionally need a security parameter, a
joint or conditional source model, an explicit seeded or multi-source
extractor, output length and statistical-error parameters, and either a game
or an ideal functionality with an adversary, environment, and simulator. The
present implementation is an audit layer and is not such a construction.

%------------------------------------------------------------------------------
\section{Implementation}
\label{sec:implementation}
%------------------------------------------------------------------------------

We provide a reference implementation in Julia as part of the Gay.jl package.

\begin{algorithm}
\caption{GF(3) Entropy Composition}
\label{alg:composition}
\begin{algorithmic}[1]
\Require Entropy sources $\mathcal{E}_1, \mathcal{E}_2, \mathcal{E}_3$
\Ensure A deterministic mixed value or rejection under the label policy
\State $t_1 \gets \tau(\mathcal{E}_1)$; $t_2 \gets \tau(\mathcal{E}_2)$; $t_3 \gets \tau(\mathcal{E}_3)$
\If{$(t_1 \oplus_3 t_2 \oplus_3 t_3) \neq 0$}
    \State \textbf{reject} (conservation violated)
\EndIf
\State $\chi_1 \gets \text{assign\_color}(t_1)$
\State $\chi_2 \gets \text{assign\_color}(t_2)$
\State $\chi_3 \gets \text{assign\_color}(t_3)$
\State \Return $\text{XOR}(\mathcal{E}_1.\text{sample}(), \mathcal{E}_2.\text{sample}(), \mathcal{E}_3.\text{sample}())$
\end{algorithmic}
\end{algorithm}

\subsection{Illustrative sensor policy}

As a non-empirical example, a policy could label three sensor classes:
\begin{itemize}
    \item \textbf{LiDAR timing jitter}: PLUS (+1)
    \item \textbf{Camera CCD noise}: ERGODIC (0)
    \item \textbf{Radar Doppler variance}: MINUS (-1)
\end{itemize}

The labels satisfy $+1 + 0 + (-1) = 0$. This says only that the policy is
balanced; source quality, dependence, and fitness for TPM seeding require
separate measurement and analysis.

%------------------------------------------------------------------------------
\section{Related Work}
\label{sec:related}
%------------------------------------------------------------------------------

\paragraph{Visual Cryptography.} Naor and Shamir's visual secret sharing~\cite{naor1995visual} uses visual representations but for different purposes.

\paragraph{Entropy Visualization.} Prior work on entropy visualization~\cite{entropy_viz} focuses on display rather than algebraic structure.

\paragraph{Composable Entropy.} Dodis et al.~\cite{dodis2004extractors} study entropy extraction but not colored composition.

%------------------------------------------------------------------------------
\section{Conclusion}
\label{sec:conclusion}
%------------------------------------------------------------------------------

We have specified a GF(3)-valued audit and presentation layer for entropy-source observations. Its conservation identities are elementary and executable, while its colors remain representations rather than security evidence. The counterexample in \Cref{thm:security} prevents a balanced label policy from being mistaken for entropy extraction. Future work must begin with a precise source model and extractor construction before making cryptographic security claims.

%------------------------------------------------------------------------------
\section*{Acknowledgments}
%------------------------------------------------------------------------------

[Acknowledgments will be added in the final version.]

%------------------------------------------------------------------------------
\bibliographystyle{alpha}
\bibliography{refs}

%------------------------------------------------------------------------------
\appendix
\section{Proofs}
\label{app:proofs}

\begin{proof}[Proof of \Cref{thm:conservation}]
The additive inverse axiom in $\mathbb{F}_3$ gives
$a+b-(a+b)=0$. The repository theorem
\texttt{balanced\_iff} in \texttt{lean4/gf3\_elegant.lean} states the
corresponding equivalence. The nearby theorem \texttt{char\_three} proves the
distinct characteristic-three identity $t+t+t=0$. Neither theorem mentions
entropy sources, independence, or cryptographic security.
\end{proof}

\begin{proof}[Proof of \Cref{thm:security}]
Let each source output the all-zero string with probability one. Assign the
three observations labels $+1,0,-1$. The label sum is zero, but every function
of the joint deterministic sample is deterministic and has min-entropy zero.
This proves the non-implication.

\paragraph{Formal-verification scope.} The checked-in Lean sources include
machine-checkable finite-field identities such as \texttt{balanced\_iff} and
\texttt{char\_three}. They do not formalize an entropy-source distribution,
extractor, random-oracle game, or UC ideal functionality. We therefore make no
machine-verification claim for a cryptographic theorem.
\end{proof}

\section{Connection to Bumpus-Kocsis Bound}
\label{app:bumpus}

The GF(3) conservation law connects to a deeper result in lattice theory. Bumpus and Kocsis~\cite{bumpus2021} proved that in any finite non-Boolean Heyting algebra $H$, at most $\frac{2}{3}$ of elements satisfy the law of excluded middle ($a \vee \neg a = \top$).

The tight witness is the three-element chain $\{\bot < e < \top\}$:
\begin{itemize}
    \item $\bot$ ($\tau = 0$, ERGODIC): satisfies LEM trivially
    \item $\top$ ($\tau = 0$, ERGODIC): satisfies LEM trivially
    \item $e$ ($\tau = 0$, ERGODIC): does NOT satisfy LEM ($e \vee \neg e = e \vee \bot = e \neq \top$)
\end{itemize}

The Heyting-algebra result does not imply that a software coordinator requires
an external beacon, nor does it establish entropy or authentication properties.
Any such operational requirement must be justified by a separate threat and
source model.

The Padovan residue observations in the repository are combinatorial examples.
We do not infer a physical Noether law or a representation-learning phase
transition from them.

\end{document}
